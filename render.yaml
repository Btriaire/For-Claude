services:
  - type: web
    name: montblanc-dashboard
    runtime: python
    rootDir: montblanc-dashboard
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
